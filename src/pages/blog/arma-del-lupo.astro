---
import ArchiveStory from '../../components/ArchiveStory.astro';
import { getArchiveStory } from '../../data/archive';
import Layout from '../../layouts/Layout.astro';

const story = getArchiveStory('arma-del-lupo');
---

<Layout title={`${story.title} — Alberto Cavedon`} description={story.description}>
  <ArchiveStory story={story} />
</Layout>
